我现在的需求是做个一个链上跟单平台
核心目标是：监听指定“领头地址”，在DEX上发生交易时，自动在目标链复制同样交易（或等比例交易），只做三件事：
1.支持 1–2 条链（如 Ethereum + BSC）
2.只支持 1–2 个 DEX（比如 Uniswap / PancakeSwap）
3.只做“跟单买入”，不做复杂策略（不做止盈止损、不做AI）

具体需求：
1.前端界面使用react，后端使用Go 
2.如果涉及数据库，使用mysql。
3.用户使用钱包登录

下面是流程图：
监听模块
   ↓
发现 target wallet tx
   ↓
解析是否为 swap
   ↓
判断是否符合策略
   ↓
计算跟单金额
   ↓
构造 swap tx
   ↓
发送交易
   ↓
返回 tx hash

