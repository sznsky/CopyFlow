# CopyFlow 项目状态

## 当前版本：聪明钱包信号平台 MVP

---

## 已完成功能

### 后端

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户认证 | ✅ | MetaMask 钱包连接 + SIWE 签名登录，JWT 鉴权 |
| 聪明钱数据同步 | ✅ | The Graph Uniswap V2/V3，支持种子/pair/全量三种模式 |
| FIFO 盈亏计算 | ✅ | 卖出交易匹配买入，计算 PNL / 持仓时长 |
| 6 维评分算法 | ✅ | PNL + 胜率 + 盈亏比 + 最大回撤 + 主流币占比 + 交易频率 |
| 钱包排名 | ✅ | Top 20 标记，动态更新 |
| 代币信号聚合 | ✅ | 多钱包共识买入信号，共识分算法 |
| REST API | ✅ | 钱包列表、信号列表、钱包详情等接口 |
| SmartMoney Worker | ✅ | 定时同步 + 评分 + 信号聚合，支持种子模式 |
| 跟单执行（BSC） | ✅ | PancakeSwap V2 链上跟单（已有，非当前展示重点） |

### 前端

| 页面 | 状态 | 说明 |
|------|------|------|
| Dashboard | ✅ | 今日代币机会 + 聪明钱包动态，未登录也可浏览 |
| 聪明钱包榜单 | ✅ | 评分、PNL、胜率展示，可点进详情 |
| 代币信号 | ✅ | 共识买入信号，含共识分和参与钱包数 |
| 钱包详情 | ✅ | 完整交易记录，含买卖方向和盈亏 |
| 跟单配置 | ✅ | 创建/管理跟单策略 |
| 跟单记录 | ✅ | 查看跟单历史 |
| MetaMask 登录 | ✅ | 一键连接 + SIWE 签名，退出正确断开 wagmi |
| 移动端适配 | ✅ | 响应式布局，汉堡菜单 |

---

## 面试核心亮点

### 亮点一：评分算法（最能体现技术思维）

6 个维度、有权重、有逻辑依据，不是随便打分：

| 维度 | 权重 | 为什么选这个指标 |
|------|------|----------------|
| 累计盈亏 PNL | 30 分 | 最直接衡量赚钱能力 |
| 胜率 | 20 分 | 反映判断方向的准确性 |
| 盈亏比 | 15 分 | 区分"小赚大亏"和"真正的好交易者" |
| 最大回撤 | 15 分 | 风险控制能力，回撤大说明策略不稳健 |
| 主流币占比 | 10 分 | 过滤高风险小币投机者，关注成熟市场 |
| 交易频率 | 10 分 | 活跃度，过滤休眠钱包 |

盈亏计算用 **FIFO 原则**（先买先卖匹配），是交易系统中标准的成本核算方式，有金融依据。

面试话术：
> "评分不是随意设计的。盈亏比和胜率结合才能真正识别优质交易者——胜率 90% 但每次只赚 1 块、亏 10 块的不算聪明钱。最大回撤反映风险控制，这是机构评估基金经理的核心指标之一。"

---

### 亮点二：系统架构（Go 后端分层设计）

```
HTTP 请求
    ↓
handler 层（参数校验、响应格式）
    ↓
service 层（业务逻辑：评分 / 信号聚合 / 同步）
    ↓
store 层（数据访问，封装 GORM）
    ↓
MySQL
```

Worker 和 API 服务**共享 service 层**，不重复业务逻辑：
- `cmd/api` → HTTP 请求触发 service
- `cmd/smartmoney-worker` → 定时任务触发同一套 service

面试话术：
> "分层的好处是职责清晰、易于测试。handler 只管 HTTP，service 只管业务逻辑，store 只管数据访问。Worker 和 API 复用同一个 service，避免了代码重复。"

---

### 亮点三：Web3 集成（SIWE 签名登录）

很多后端开发不了解 Web3 登录流程，这是差异化亮点：

```
前端：wagmi 连接 MetaMask → 获取钱包地址
    ↓
后端：生成 Nonce（防重放攻击）
    ↓
前端：用私钥对 "Sign-In with Ethereum" 消息签名
    ↓
后端：用 ecrecover 验证签名 → 确认地址所有权 → 签发 JWT
```

关键点：
- 不需要密码，**签名即身份证明**（非对称加密）
- Nonce 有过期时间，防止重放攻击
- 符合 [EIP-4361 SIWE 标准](https://eips.ethereum.org/EIPS/eip-4361)

面试话术：
> "传统登录是密码匹配，Web3 登录是签名验证。用户用私钥签一条消息，后端用公钥（钱包地址）验证签名，证明你确实拥有这个地址。不需要存密码，天然防钓鱼。"

---

## 核心设计决策

### 为什么用种子地址而不是全量扫描

全量扫描 Uniswap 每天有数十万笔交易，The Graph 免费层（10 万次查询/月）根本不够用。种子模式从已知高质量地址出发，每天只需几十次查询，高效且精准。

### 为什么不做自动跟单（当前版本）

链上自动跟单涉及真实资产，延迟、滑点、私钥安全等问题复杂。当前版本专注**信号发现和展示**，帮助用户做决策，跟单作为可选的下一步。

---

## 数据流

```
seed_wallets（config.yaml 手动维护）
    ↓
The Graph Uniswap V2/V3（按地址过滤）
    ↓
wallet_trades 表
    ↓
FIFO PNL 计算 → 6 维评分 → smart_wallets 表
    ↓
代币信号聚合 → token_signals 表
    ↓
前端展示
```

---

## 待完成 / 后续规划

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 高 | 填入真实种子地址 | 从 Nansen/Arkham 整理 20-50 个已知大户地址 |
| 高 | 获取 The Graph API Key | thegraph.com/studio 免费注册 |
| 中 | 代币元数据 | 接入 CoinGecko 补充代币名称/logo/市值 |
| 低 | 以太坊链跟单执行 | 扩展现有 BSC 跟单到 Ethereum + Uniswap |
| 低 | 早期项目猎手 | 接入 Dexscreener 发现早期小币种买入者 |
